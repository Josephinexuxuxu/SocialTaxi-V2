import React from 'react';
import Navigator from './routes/Navigator';
//import HomeNavigator from './routes/Navigator';
//import OrderNavigator from './routes/Navigator';

const Root = () => <Navigator />;

export default Root;
